import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leeunks.ketojournal',
  appName: 'Nutritional Ketosis Journal',
  webDir: 'dist'
};

export default config;
